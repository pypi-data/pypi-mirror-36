#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['pycgms']

package_data = \
{'': ['*']}

install_requires = \
['pcse >=5.4']

entry_points = \
{'console_scripts': ['pycgms = pycgms:start']}

setup(name='pycgms',
      version='0.1.0',
      description='The PyCGMS package provides a python implementation of the crop simulation',
      author='Allard de Wit',
      author_email='allard.dewit@wur.nl',
      url='https://github.com/ajwdewit/pyCGMS',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
     )
