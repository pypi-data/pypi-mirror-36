# Setuptools silently include all files stored under a git's source tree, so
# there's no point in repeating that.

include .version.txt

recursive-include src/twweb/static/
recursive-include src/twweb/templates/

recursive-exclude ci *
recursive-exclude docs/build *
recursive-exclude tests *
recursive-exclude .git *

exclude .gitignore

recursive-exclude * *.pyc *.pyo *.mo
