{
  "definitions": {
    "custom": {
      "id": "#custom",
      "type": "object",
      "properties": {
      {% for i in range(1, 11) %}
        "Tenant{{i}}": {
          "scope": "NONE",
          "title": "Tenant{{i}}",
          "type": "string"
        },
        "Role{{i}}": {
          "scope": "NONE",
          "title": "Role{{i}}",
          "type": "string"
        }{% if not loop.last %},{% endif %}
      {% endfor %}
      },
      "required": []
    }
  }
}
