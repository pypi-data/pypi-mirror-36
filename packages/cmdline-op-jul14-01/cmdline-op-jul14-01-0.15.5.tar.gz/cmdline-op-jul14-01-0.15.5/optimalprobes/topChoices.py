def funcTopChoices(args):
	print args
	# open the db file
	# read the scores
	# create a file with scores, num_probes, num_distances, choices
	# print the highest "n" scores and its accompanying columns
