pgt
====

Pages generate tool.

```
Code:
@{[_fn] | \\name value \\name value ... }
@<<!+ name ? (value) >>
```

```
Command:
$ pgt 
$ pgt --all
$ pgt --init\n
$ pgt --import jQuery underscore
$ pgt --import $github_user.$scaffold
```